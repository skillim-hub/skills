# Hebrew QA Log

## Scope

Reviewed `SKILL_HE.md` and Hebrew-facing date guidance in the reference files.

## Changes applied

- Replaced Hebrew-facing date display examples from `DD-MM-YYYY` to `DD/MM/YYYY`.
- Replaced example dates such as `10-03-2026`, `08-06-2026`, and `14-05-2026` with `10/03/2026`, `08/06/2026`, and `14/05/2026`.
- Added a localization section requiring Hebrew documents to display dates as `DD/MM/YYYY` while storing internal dates as `YYYY-MM-DD`.
- Kept neutral plural imperative forms such as `בדקו`, `שמרו`, `הגישו`, `השתמשו`, and `אל תשמרו`.
- Replaced standalone `SMS` with `מסרון` in Hebrew prose.
- Replaced standalone `GPS` with `מערכת איתור` in Hebrew prose.
- Clarified file references as `קובץ PDF` where needed.
- Confirmed that technical prose contains no nikud.
- Confirmed use of professional Israeli terms: `דוח`, `קנס`, `הסבת דוח`, `גבייה`, `הצמדה וריבית`, `חוק עזר עירוני`, `ח.פ.`, `ע.מ.`, `הנהלת חשבונות`, `רואה חשבון`, `יועץ מס`.
- Confirmed `₪` appears in human-facing money examples.
- Preserved common official-service terms where Hebrew replacement would reduce clarity, such as `פנגו`, `כביש 6`, and `קובץ PDF`.

## Open items

- Verify any future municipality-specific wording against the current wording used by the relevant authority before submitting a real request.
- Confirm accounting treatment with a qualified accountant when a business uses the workflow for material amounts.
