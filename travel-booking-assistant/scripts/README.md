# Scripts

- `travel-booking-assistant-cli.py` is a script wrapper for the installable package CLI.
- `test_travel_booking_assistant_client.py` contains the pytest suite.
- `examples/` contains runnable scenario scripts.

Install the package with `pip install -e .` before running examples from outside the project root.
