# Hebrew QA Log

Date: 2026-06-04

## Scope

Reviewed `SKILL_HE.md` for professional Israeli usage, neutral imperative phrasing, local formatting, and technical clarity.

## Changes Applied

| Area | Change |
|---|---|
| Terminology | Replaced broad use of foreign sales terms with `פנייה`, `פניות`, `הפונה`, and `תור טיפול` where natural. |
| Skill wording | Replaced references to the skill as a borrowed term with `המיומנות`. |
| Customer systems | Replaced generic foreign shorthand with `מערכת ניהול קשרי לקוחות` where the Hebrew term is clear. |
| Marketplace wording | Replaced borrowed marketplace phrasing with `זירות מסחר`. |
| Email wording | Replaced casual wording with `דואר אלקטרוני`. |
| Response targets | Replaced service-level shorthand with `יעד זמן תגובה`. |
| Date localization | Changed example date format to `04/06/2026 09:30`. |
| Currency localization | Kept monetary examples in ₪. |
| Voice | Kept instructions in neutral imperative phrasing such as `נתב`, `השתמש`, `בדוק`, `שמור`, and `העבר`. |
| Grammar | Corrected agreement in the introduction, including `המיומנות מתאימה` and `היא תומכת`. |
| Technical prose | Confirmed no nikud appears in Hebrew technical prose. |

## Remaining Terms Kept Intentionally

| Term | Reason |
|---|---|
| `WhatsApp` | Product name used by Israeli businesses. |
| `Lead Router` | Package name and title. |
| Code identifiers such as `language`, `region`, `product_interest` | Required field names used by the client, CLI, and examples. |
| Language codes such as `HE`, `EN`, `RU`, `AR` | Stable integration values. |


## v3 web-validated Hebrew QA pass - 04/06/2026

| Area | Change |
|---|---|
| Date localization | Corrected the visible example from `04-06-2026` to `04/06/2026`. |
| Tax terminology | Added a practical מע״מ note using `רשות המסים`, `חשבוניות`, and `שיעור המע״מ התקני`. |
| Lead terminology | Replaced broad uses of `לידים` with `פניות` where a natural Hebrew business term was clearer. |
| Imperative voice | Converted key operating guardrails to plural imperative forms such as `אספו`, `הפרידו`, `שמרו`, and `הציגו`. |
| Agreement fixes | Corrected `פנייה שיווקי` to `פנייה שיווקית` and `פניות ... מזוהים` to `פניות ... מזוהות`. |
| Technical prose | Confirmed no nikud was added to Hebrew technical prose. |
